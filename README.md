# Cross-Platform Keylogger Detector

A Python-based tool to detect potential keyloggers on Windows and Linux systems.

## Features
- Process monitoring for known keyloggers
- Network port checking
- Filesystem scanning
- Report generation

## Installation
```bash
git clone https://github.com/ysraelll/key_logger
cd keylogger-detector
pip install -r requirements.txt
